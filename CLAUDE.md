# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 저장소

한결(Hangyeol) **회사 홈페이지 5페이지**.
브랜드 철학을 앞세운 소개 사이트이고, 1차 오프라인 모임 랜딩은 그중 한 페이지다.
Next.js 16 (App Router) · React 19 · TypeScript · Tailwind v4(preflight만, `@theme` 없음).
shadcn은 쓰지 않는다. 경로 alias `@/*` → `src/*`.

기획 문서는 이 저장소가 아니라 옵시디언 볼트 `C:\second_brain\한결\`에 있다.
필요한 문서만 그때그때 읽는다 — 아래 **문서 라우팅** 참조. 볼트 전체를 미리 훑지 말 것.

### 라우팅 구조 (5개 경로)

| 경로 | 파일 | 성격 | 카피 원문 |
|---|---|---|---|
| `/` | `page.tsx` + `HomeStage`(+`HomeHero`·`HomeDialog`) + `FunnelNav` | 회사 메인 = **대화형 무대** + 마무리 2화면. 인트로 → 질문 3개(답 클릭 → 반응+영역 카드) → 허브(신청/더 알아보기) → 스크롤로 1차 모임 정보·CTA. StickyBar 없음 | 브랜드 선언문·핵심 명제 존댓말화 |
| `/mission` | `src/app/mission/page.tsx` | 목표·비전·나무의 결·이름의 이중 의미 | §5.1 · §5.2 · §6 · §4.1 · §4.5 · §4.6 |
| `/why` | `src/app/why/page.tsx` | 왜 가치관인가 — 구조적 결함 4가지 + 연구 근거 | §2.1 · §2.2 · §3.2 · §3.3 |
| `/principles` | `src/app/principles/page.tsx` | 5개 원칙 + 그라운드룰 + 비공개 정보 | §8(8개 중 5개) · §3.5 · 시장조사 §9 |
| `/events/1` | `src/app/events/1/page.tsx` | 1차 모임 랜딩(신청). NAV에 없고 CTA로만 진입 | `_1차 오프라인 소개팅.md` |

`NAV`(헤더·푸터 공유)와 `EVENT_HREF`는 `src/lib/site.ts`에 있다.

## 🔒 대외비 — `01_brand_philosophy.md`

이 문서는 **저장소 안에 있지만 대외비**다. 웹에 나가는 것은 아래 절뿐이다.

- **공개 가능**: §2.2 · §3.2 · §3.3 · §4 · §5 · §6 · §7 · §8
> ⚠️ **이 목록에 실제 값을 옮겨 적지 말 것.** 무엇이 금지인지만 가리키고, 숫자와 고유명사는
> 원본 문서에만 둔다. 여기(CLAUDE.md)는 저장소에 커밋되므로, 값을 적는 순간
> "숨기라고 적어둔 것을 이 파일이 공개하는" 상태가 된다. 실제로 그랬어서 걷어냈다.

- **절대 노출 금지** (값은 원본 문서에서 확인할 것):
  - 매칭 가중치 표 — 퍼센트 수치와 항목명 **전부**
  - 가설 판정 기준 — 판정 일수, 조건 코드, 기각 여부
  - 경쟁사 실측 전환율과 **업체명 일체**
  - 시장 구조 비교표 · 원가/손익 (손익분기·적자·대관 단가)
  - **§9 실무 적용 전체** (온보딩·자가진단·의사결정 체크리스트·브랜드 언어 가이드)
  - §8.1의 정원 확정 주석, 원칙 2의 적자 감수 문장
- **내부 용어 금지**: 결카드 · 로테이션 · 매칭 유사도 · 일치(율)
  → `/principles` 원칙 1의 금지 항목은 원문에 그 용어가 들어간 예시가 붙어 있는데,
  예시를 잘라내고 `숫자를 단독으로 노출하는 것`만 싣는다.

## 카피 규칙 — 문서는 내부 언어, 웹은 손님 언어

`01_brand_philosophy.md`가 **원본**이고 웹은 그 **번역본**이다.
문서는 선언문체("~다")로 그대로 두고, **웹에 나가는 카피는 존댓말("~습니다")로 옮긴다.**

옮길 때 **바꿔도 되는 것은 말투뿐이다.** 아래는 그대로 간다:

- 사실·숫자·출처 (`Joel et al. 2020, PNAS` 같은 인용 표기 포함)
- 금지어 규칙("소개팅앱"·"헌팅"·술자리 뉘앙스) 과 경쟁사 폄하 금지(§2.1)
- 요약·창작·연결구 추가 금지 — 말투만 바꾸고 논지를 바꾸지 않는다
- 대외비 때문에 문장을 잘라야 하면 **잘라내기만 하고 다시 쓰지 않는다**(그 사실을 주석으로 남길 것)

**존댓말로 바꾸지 않는 예외 4종.** 각 파일 상단 주석에 이유를 적어 두었으니 지우지 말 것:

| 예외 | 어디에 | 왜 |
|---|---|---|
| 원칙 5개의 `title` | `/principles` | 강령이다. 홈이 원칙 1·3·5의 제목을 그대로 인용하므로 고치면 두 곳이 어긋난다 |
| 미션 문장 (`…대화를 설계한다.`) | `/mission` h1 | 바로 아래 `WORDS`가 이 문장을 여섯 단어로 쪼개 분석한다. `설계한다`를 고치면 `word` 값과 어긋난다 |
| 표의 칸 · 금지 항목 목록 | 전 페이지 | 명사구라 존댓말로 늘이면 표가 무너진다 (`deny`, `factDesc`, `GROUND_RULES`, `HIDDEN`, `ALLOWED`, `PHASES.goal`, `LAYERS`, `ANATOMY`) |
| 큰따옴표 안의 인용문 | 전 페이지 | 남의 말이거나 원문 인용이다 (`echoQuote`, "제한된 시간에 최대한 많은 이성을…") |

`/why`의 결함 제목 4개(`사람을 스펙 카드로 환원한다` 등)도 유지한다 —
**기존 포맷**을 서술하는 말이라 존댓말로 바꾸면 한결이 그렇게 한다는 뜻이 된다.

1인칭은 **"저희"** 로 통일한다("우리" 아님).

### 화면에 쓰지 않는 말

| 쓰지 말 것 | 대신 | 이유 |
|---|---|---|
| 미션 | **목표** | 화면 문구 한정. 경로 `/mission`과 파일명은 그대로 둔다(공유된 링크가 깨진다) |
| 원칙과 안전 | **원칙** | 라벨은 `site.ts`의 `NAV` 한 곳에서 온다. 경로는 `/principles` 그대로 |
| 실험 · 실험체 · 가설 검증 | **모임 · 도입** | 읽는 사람이 자기를 실험 대상으로 느낀다 |
| 소개팅앱 · 솔로파티 등 **업종·서비스 이름** | **"기존의 만남 서비스"** 처럼 범주로만 | 금지어이자 §2.1 경쟁사 폄하 금지. 잠재 고객 상당수가 그 서비스 이용자라 전략적 자해다 |
| 성공의 정의 · 실패의 신호 | (싣지 않음) | 내부 판단 기준이라 손님이 알 필요가 없다 |

## 항상 지킬 것

- **브랜드**: 한결. 가치관·성격의 *결*이 맞는 사람을 오프라인에서 연결한다.
- **슬로건**: 반드시 전문 **"결국에는 결이더라, 한결같이"** (쉼표 포함).
  축약형("결국에는 결이더라") 단독 사용 금지. 유일한 출처는 `src/lib/site.ts`의 `SITE.slogan`이고
  카피에 문자열로 박아 넣지 않는다. 히어로만 예외 — 2줄 + 워드플레이로 분해해 보여준다.
- **현재 단계**: 앱을 만들기 전, 오프라인 소개팅 3회로 가설을 검증하는 중. 1차는 합정 확정.
- **운영자**: 이현우(기획·진행, CEO) · 여동현(기획·기술, CTO). 연락처는 `site.ts`의 `CONTACT`.
- **톤**: "소개팅앱"(띄어쓴 "소개팅 앱"도 포함) · "헌팅" · 술자리 뉘앙스 금지.
- **경쟁사 폄하 금지** (§2.1). 우리가 겨누는 것은 사람도 업체도 아니라 **오직 "설계"**다.
  "기존 포맷은 나쁘게 만들어진 게 아니라 다른 목적에 맞게 잘 만들어졌다."
  ❌ "진지하지 않은 사람들이 간다" / "만남이 가볍다" / "프로필이 가짜다" — 틀렸을 뿐 아니라
  잠재 고객 상당수가 그 행사 참가자라 **전략적으로 자해**다. `src/app/why/page.tsx`가 대조 기준.
- **숫자는 추측 금지**: 가격·정원·장소·시간·환불은 `src/lib/event.ts`에서 가져온다(아래 코드 구조).
  광고와 실제가 다른 것은 이 시장의 대표적 불만 요인이다.
- **"Kin" 주의**: `_홍보.md`의 인터뷰 대본에 남아 있는 구 명칭. 현재 이름은 **한결**이므로 그대로 쓰지 말 것.

## 문서 라우팅 (필요할 때만 읽기)

경로 접두사: `C:\second_brain\한결\`
obsidian MCP로도 접근 가능 — `mcp__obsidian__vault_read`에 `한결/<파일명>` 전달.

| 이 작업을 할 때 | 읽을 파일 |
|---|---|
| 로고·슬로건·브랜드 카피 | `_브랜딩.md` |
| 홈페이지 섹션 구성과 순서 | `_홈페이지.md` |
| 정체성 문단, 타겟 페르소나 | `_필수 정보.md` |
| 가격·정원·장소, 참가 조건, 사전질문 10주제 | `_1차 오프라인 소개팅.md` |
| "진행 순서" 섹션, 행사 흐름 설명 | `_1차 오프라인_타임테이블.md` |
| FAQ·차별점·안전 문구의 근거 | `_시장조사.md` |
| 예산, 공간 단가, 그라운드룰, 경쟁사 비교 | `한결_시장조사_운영자관점.html` (아래 주의) |
| 인스타·명함·인터뷰 대본 | `_홍보.md` |
| 할 일 현황 확인 | `_진행상황.md` |

### 볼트 취급 규칙

- 볼트는 사용자 소유 원본이다. **읽기가 기본**이고, 수정은 명시적 요청이 있을 때만 한다.
- 홈페이지 작업이 끝나면 `_진행상황.md`의 "해야 할 것" 체크박스를 갱신할지 사용자에게 확인한다.

### 대용량 파일: `한결_시장조사_운영자관점.html` (83KB)

**통째로 읽지 말 것.** 필요한 섹션만 grep으로 뽑는다:

```bash
grep -a -A 40 '주요 경쟁자 비교' "C:/second_brain/한결/한결_시장조사_운영자관점.html"
```

포함된 섹션 (grep 키워드로 사용):
주요 경쟁자 비교 · 플랫폼 입점 비용 · 남녀 차등 요금 · 합정·홍대 공간 8곳 ·
식음료 1인 10,000원 · 인쇄물 · 현행 예산 · 실측 대관 반영 · 채널별 평가 ·
불만 유형별 언급 빈도 · 원문 인용 · 그라운드룰 · 2부 재설계 · 3부 확정 ·
1부 가치관 질문 사전 공개 · 운영 프로토콜 · 모드파티에서 그대로 가져올 것

## 개발 명령

```bash
npm run dev     # 개발 서버 (localhost:3000)
npm run build   # 프로덕션 빌드 + 타입 검사
npm run lint    # ESLint
npx tsc --noEmit  # 타입만 검사
```

⚠️ Next 16은 **같은 프로젝트 디렉터리에 두 번째 dev 서버를 거부**한다(`-p`로 포트를 바꿔도 소용없다).
이미 떠 있는 서버가 있으면 그쪽에 붙어 확인하고, 남의 프로세스를 죽이지 않는다.
dev 서버가 떠 있는 동안 `npm run build`를 돌리면 `.next`가 충돌한다 —
`distDir`를 임시로 바꿔 빌드하고 반드시 되돌릴 것(`next build`는 `tsconfig.json`도 함께 고쳐 놓는다).

## 디자인 원본

`design/design_handoff_hangyeol_landing/`은 Claude Design 핸드오프 번들이다.
**여기 파일은 고치지 않는다** — 디자인 원본이자 대조 기준이다.

- `README.md` — 하이파이 스펙. 색 토큰, 타이포 스케일, 9.5초 인트로 타임라인이 초 단위로 적혀 있다.
  UI 수치를 바꾸기 전에 여기부터 읽을 것.
- `hangyeol-landing.dc.html` — 확정 디자인. Claude Design 런타임(`support.js`)에 의존하므로
  브라우저로 열어 **눈으로 대조하는 용도**다. 배포 코드가 아니다.
- `support.js` / `image-slot.js` — 프로토타입 전용. 프로덕션에 옮기지 말 것.

레이아웃을 손볼 때는 `hangyeol-landing.dc.html`을 file:// 로 띄워 `localhost:3000`과 나란히 비교한다.
핸드오프가 대응하는 것은 `/`와 `/events/1`뿐이다 — 문서형 3페이지(`/mission` `/why` `/principles`)는
핸드오프에 없고 globals.css의 공통 프리미티브 위에서 새로 만든 것이다.

⚠️ 핸드오프의 인트로는 **하트**(`heartDraw`)다. 그건 폐기된 안이다 — 위 "히어로 인트로" 참조.
접근성 때문에 의도적으로 어긋나게 둔 값도 있다(`--faint`, `--num`, `.display` 하한 44px).
핸드오프에 맞추겠다고 되돌리지 말 것.

## 코드 구조

- `src/lib/event.ts` — **행사의 확정 사실이 모여 있는 단일 출처.**
  `EVENT`(날짜·시간·장소·정원·가격·나이) · `REFUND`(환불 규정) · `AGE_RANGE`.
  카피에 숫자를 넣을 때 하드코딩하지 말고 여기서 가져온다. 날짜가 확정되면 `EVENT.date` 한 곳만
  고치면 히어로 칩·정보표·하단 바·메타 description에 모두 반영된다.
- `src/lib/site.ts` — 브랜드명·슬로건·NAV·행사 CTA. 슬로건은 여기서만 나온다.
- `src/app/globals.css` — 디자인 토큰과 **공통 프리미티브 전부**.
  `.band` `.band--dark` `.eyebrow` `.display` `.lede` `.link-arrow` `.stagger` `.card` `.pill` …
  페이지 전용 스타일은 CSS Module(`mission.module.css` 등)로 만들고 globals.css는 건드리지 않는다.
  - **팔레트(2026-08 리브랜딩)**: 오프화이트 `--paper #F5F1E8` 바탕 + 딥그린
    `--green-900 #1E3A2F` 잉크. **테라코타 `--terra #C97B5A`는 장식(선·점·바) 전용** —
    종이 위 2.88:1이라 글자 금지, 글자에는 `--terra-ink #9A5637`(4.95:1)를 쓴다.
    `#fff`·`#111`을 다시 하드코딩하지 말 것 — 전부 베이스 토큰을 참조한다.
  - **크기·여백은 토큰에서 고른다. px을 직접 적지 말 것.**
    고정 `--fs-1`~`--fs-5`(14·16·18·21·26px), 반응형 제목 `--fs-6`~`--fs-8`·`--fs-display`·
    `--fs-display-page`, 여백 `--sp-1`~`--sp-7`, 라벨 자간 `--ls-label`(0.08em 1종),
    행간 `--lh-display/title/ui/body/prose`(1.1/1.3/1.5/1.7/1.85), 헤더 높이 `--header-h`.
    예외로 남는 일점물: `.hero__headline`(100px)·워드플레이 2종·홈 퍼널 `.q`(하한 34px —
    320px에서 6자 어절이 넘치지 않는 하한이다).
    `padding`·`left`·`grid-template-columns`는 토큰화하지 않았다 —
    정렬을 떠받치는 값이 섞여 있다(`.timeline__line`의 `left:90px` ↔ `.timeline__row`의 `112px`).
  - **힌지식 4단 구조**를 예외 없이 반복: eyebrow(작은 라벨) → 큰 제목 → 본문 → 링크/CTA
  - **밴드 교차**: 종이 → 딥그린 → 종이 → 딥그린. `.band--dark`는 배경색이 아니라 **토큰을
    반전**시키므로 안의 요소는 자동으로 따라온다(이름은 그대로 둔다 — "dark"는 명도 역할명).
    딥그린 밴드 안에서 색을 하드코딩하면 종이 위 종이가 된다.
  - 토큰 대비는 전 조합 계산돼 있다(각 토큰 옆 주석). `--faint`(4.79:1)와
    `--num`(19px 이상 굵은 글자 전용)을 미달값으로 되돌리지 말 것.
  - `word-break: keep-all`이 `body`에 있다. 없으면 한글이 어절 중간에서 끊긴다.
- **홈 무대(HomeStage)** — 인트로와 **체험형 시퀀스**(9차 A안:
  a1→t1→a2→t2→a3→why→cmp→a4→hub)를 한 화면에서 잇는 상태 머신.
  가치 2지선다 3개에 답하고, 마지막 질문은 **이유까지 고른다** — 원칙 1을 동작으로
  보여주는 구조라 질문을 바꿔도 이유 3택 구조는 유지할 것. 질문 근거는 볼트
  사전질문 10주제, 숫자(10명·9분)는 event.ts(rotationPartners/rotationMinutes).
  교체용 세트 B~E는 9차 플랜 파일. 비교 화면(cmp)에 수치를 지어내지 않는다.
  함정 4가지를 지킬 것(전부 실측으로 확인된 것들):
  1. 레이어는 **상시 마운트 + opacity/visibility + inert**. 언마운트하면 인트로가
     재생되고 스냅 높이가 요동친다.
  2. `.layer`에 **position/z-index 금지** — 스태킹 컨텍스트가 생기면 안의 fixed
     건너뛰기(z:61)가 헤더(z:20) 아래 갇혀 클릭이 막힌다.
  3. 인트로 끝은 `animationend`(`ulIn`)로 감지 — setTimeout 금지(스킵과 어긋난다).
     `finish()`는 무한 반복 애니메이션에 throw하므로 SkipIntroButton의 필터 유지.
  4. **자동 진행 없음**(7차 소유자 결정 — 읽는 속도를 강제하지 않는다). 되돌림·비교·
     착지 전부 버튼 진행. 되돌림 둘째 줄과 cmp 마침 문장은 Typewriter — 시각 타이핑은
     aria-hidden, 전문은 sr-only 즉시 제공, RM은 즉시 표시.
  5. 진행 복원의 진실은 **sessionStorage(`dialog-phase`·`dialog-chosen`)**다 —
     html[data-dialog-phase] 속성은 전체 로드의 첫 페인트용 보조일 뿐이다.
     서브페이지 뒤로가기(SPA 내비)는 문서를 재로드하지 않아 인라인 스크립트가
     안 돌므로, 속성만 읽으면 그 경로의 복원이 통째로 빠진다.
     속성 제거는 복원 phase가 **커밋된 뒤**(별도 이펙트) — 먼저 걷으면 히어로가
     한 프레임 비친다. 이펙트 안에서 flushSync 금지(하이드레이션 경고).
  React는 이 속성들을 렌더에서 읽지 않는다(하이드레이션). 무대 내부에 `data-reveal` 금지.
  반응 화면의 진행 버튼 라벨은 r1·r2 "다음 질문", r3 "마무리 보기"로 갈린다 —
  통일하면 마지막에서 거짓말이 된다. q2·q3에는 "이전 질문으로"가 있다.
  대화 도중 서브페이지로 간 방문자에게는 `BackToDialog` 칩("하던 대화로 돌아가기")이
  /mission·/why·/principles 상단에 뜬다(마운트 후 sessionStorage 판정 — hub·미진행은 숨김).
  헤더 로고 오른쪽의 "홈" 링크는 심볼만으로는 홈 링크임이 안 읽힌다는 피드백의 산물이다.
- **홈 퍼널 스냅** — `html:has([data-funnel])`이 루트에 `scroll-snap-type: y proximity`를
  건다. ⚠️ mandatory 금지(Footer 도달 불가/소형 폰 함정), `.screen`은 `min-height`+`svh`
  (dvh 금지 — 주소창 개폐마다 스냅 지점이 흔들린다). 진행 표시는 `FunnelNav`(IO 1개가
  점 레일과 모바일 진행 바를 함께 구동 — 진실을 둘로 나누지 말 것).
- `src/components/SiteHeader.tsx` — 모바일 오버레이는 **포털로 body에** 렌더한다.
  ⚠️ 헤더 안으로 되돌리면 헤더의 backdrop-filter가 fixed 자손의 컨테이닝 블록이 되어
  메뉴가 64px 띠로 깨진다(실제 결함이었다). 회전으로 768px를 넘으면 matchMedia 리스너가
  메뉴를 닫는다 — 없으면 body 잠금·inert가 남아 페이지가 죽는다.
- `src/components/PageEffects.tsx` — 스크롤 리빌·헤더 축소. 클라이언트 부수효과 전용.
  앵커 이동은 CSS(`scroll-behavior: smooth` + `scroll-padding-top: var(--header-h)`)가
  맡는다 — JS 수동 보정(-56px)을 되살리지 말 것.
  ⚠️ **두 가지 함정이 있다. 둘 다 건드리지 말 것.**
  1. **`usePathname` 재스캔** — 이 컴포넌트는 layout에 있어 클라이언트 라우팅 중 언마운트되지 않는다.
     `useEffect`의 의존성 배열이 `[]`이면 마운트 시점 DOM만 스캔하므로, 헤더 링크로 이동한
     페이지의 `[data-reveal]`은 **영원히 발화하지 않는다**. `[pathname]`이 그래서 필요하다.
  2. **`[data-bar]`/`[data-line]`은 부모를 관찰한다** — 시작 상태가 `scale(0)`이라 면적이 0이고,
     자기 자신을 관찰하면 IntersectionObserver가 절대 교차하지 않는다.
     새 막대를 추가할 때는 반드시 면적 있는 부모로 감쌀 것(`.chainWrap`, `.seats__cell`가 그 예다).
- `src/components/SiteHeader.tsx` — 모바일 오버레이는 진짜 모달이다. 열릴 때 닫기 버튼으로 포커스를
  옮기고, `<main>`·`<footer>`·헤더 상단 줄에 `inert`를 걸고, 닫히면 햄버거로 포커스를 돌린다.
  오버레이가 헤더 **안에** 있어서 상단 줄까지 꺼야 햄버거가 탭 순회에서 빠진다.
- `src/app/layout.tsx` — `INTRO_SEEN_SCRIPT`가 하이드레이션 **전에** `<html>`에 속성을 붙이므로
  `<html>`의 `suppressHydrationWarning`이 필수다. 지우면 재방문 시 hydration mismatch가 난다.

## 히어로 인트로 (`/` 전용)

**인트로는 로고 입모양 + 줌인이다. 하트를 되살리지 말 것.**

- `HomeHero.tsx`의 `STROKE`는 `public/hangyeol-logo-v2.png`의 실제 획을 픽셀에서 재어 100×100
  viewBox로 옮긴 것이다 — 위쪽에 로브가 있고 아래 끝이 안쪽으로 말리는 "C"자 두 짝 + 위쪽 점 2개.
  오른쪽은 `translate(100 0) scale(-1 1)` 미러다.
- ⚠️ **두 끝점이 대칭축(x=50)에 닿으면 안 된다.** 닿는 순간 미러링된 두 획이 위아래에서 맞물려
  하트가 된다. 실제로 예전 path가 끝점을 `(50,90)`·`(50,22)`에 두는 바람에 인트로가 명백한
  하트를 그렸다(핸드오프 프로토타입의 `heartDraw`를 리스케일한 것이었다).
  `heart` 문자열로는 절대 안 잡히니 **도형을 직접 렌더해서 확인**할 것.
- `speakL`/`speakR`의 진폭은 하트 방지 장치가 아니다. 형태는 오직 `STROKE`가 정한다.
- `prefers-reduced-motion`에서는 `.hero__intro`가 `display:none`이고,
  워드플레이의 겹친 두 레이어 중 최종 상태만 남는다(globals.css 맨 아래 블록). 지우지 말 것.

## 아직 안 된 것

코드는 **값이 비어 있어도 안전하게 동작하도록** 짜여 있다. 값만 넣으면 켜진다.

- **폼 접수 주소** — `.env.local`의 `NEXT_PUBLIC_FORM_ENDPOINT`가 비면 제출 버튼이 잠기고
  "곧 열립니다"가 뜬다. 폼 백엔드 서비스(Formspree 등) 가입 후 발급받은 주소를 넣는다.
- **배포 도메인** — `NEXT_PUBLIC_SITE_URL`이 없으면 `http://localhost:3000`으로 대체된다.
- **카카오톡 채널** — `site.ts`의 `CONTACT.kakao`가 비어 있어 푸터에 그 줄이 안 나온다.
- 행사 날짜는 **"10월 중"까지만 확정**(2026-08). 일자가 정해지면 `EVENT.date` 한 곳만 고치고,
  그때 `/events/1`에 `Event` JSON-LD를 추가할 수 있다(`startDate`가 ISO 날짜여야 한다).
- 로고 SVG 미확보 — 아직 351×489 PNG를 쓴다.
- 사업자 정보 표시 여부는 소유자 판단 영역이다(계좌로 참가비를 받는다).

## 사용자 판단이 남은 것

- **밴드 분량 불균형** — 순서(종이→딥그린)는 5페이지 다 맞지만 높이가 튄다.
  `/principles` 첫 밴드 3425px(화면 3.8개), `/why` 딥그린 밴드 2683px, `/mission` 매니페스토 2454px.
  섹션을 쪼개 밴드를 하나 더 넣을지는 편집 판단이라 손대지 않았다.
- **`/events/1`만 `.lede`가 없다** — 제목 아래가 16px `.body-text`라 4단 구조의 셋째 칸이 혼자 작다.
- **문서형 3페이지의 h1 = h2 = 58px** — 페이지 대표 제목이 섹션 제목보다 커 보이지 않는다.
- **`/mission` 매니페스토 대비 18.6:1** — 순백 텍스트가 2454px 이어져 헤일레이션이 생긴다.
  이 블록만 `--lead`로 낮추는 안이 있으나 강조 의도가 있어 보류.
> ✅ 해결됨 — `01_brand_philosophy.md`와 `/.claude/`는 `.gitignore`에 등록돼 있고
> `git ls-files`로 **추적되지 않음을 확인했다**(2026-08). 원격은
> `github.com/aisystemdh/hangyeol_web`이며 한 번 푸시되면 히스토리에서 지우기 매우 어렵다 —
> **`.gitignore`의 대외비 항목을 절대 건드리지 말 것.**
