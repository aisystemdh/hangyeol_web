# 핸드오프 2026-09-05 (정리 작업은 2026-09-03에 수행, 그 뒤 변경 없음)

## 1. 지금 하던 이슈
- #2 저장소 정리 — 브랜치 `main` 직접(정리 작업이라 예외). 아래 체크 남음:
  - `.env.local`에서 `NEXT_PUBLIC_FORM_ENDPOINT` 줄 삭제 (사람이 — 도구가 env 편집을 막음)
  - `CLAUDE.md`의 라우팅·API·DB·환경변수 표를 `docs/`로 옮기고 링크만 남기기 (지금은 문서 절만 추가된 상태)
  - Vercel 프로덕션 env 추가: `HANGYEOL_BANK_NAME/ACCOUNT/HOLDER`, `HANGYEOL_BIZ_MAILORDER`, `NEXT_PUBLIC_META_PIXEL_ID` — 없음 확인됨(`vercel env ls production`)
  - 프로덕션 Neon `schema_migration`에 001~004 기록 여부 확인. 없으면 INSERT. **확인 전에 `npm run db:migrate`를 돌리지 말 것**(003이 answer_pre·participant를 drop)

## 2. 이번 세션에서 끝난 것
- `5e05996` 저장소 정리 커밋 → `origin/main` 푸시됨. `.claude/` 공유(훅·스킬·settings.json), `docs/architecture.md`(코드 전수 지도 618줄), CLAUDE.md 개정, AGENTS.md·옛 워크플로 삭제, `design/hangyeol-intro-motion.mp4` 추가.
- GitHub: 라벨 P0·P1·P2·design, 마일스톤 3개, **이슈 #2~#23 (22건)** 등록. 전체 계획은 `C:/Users/hyund/.claude/plans/1-generic-patterson.md`.
- 로컬: `~/projects/hangyeol-knowledge-base`를 `origin/main`으로 리셋(대표가 8/27 이력 재작성). 바탕화면 KB 클론 삭제. `로드맵 1.png`는 KB 로컬 루트에 untracked로 둠.
- 전역: 상태줄(`~/.claude/statusline.js`)이 모델·폴더·컨텍스트 토큰 표시, 180k 넘으면 `⚠ handoff`.
- Vercel: 프로젝트가 GitHub `aisystemdh/hangyeol_web`에 **이미 연결**돼 있음(`git-main` 도메인 존재). main push = 배포. CLI `--prod`와 수동 alias는 쓰지 않는다(CLAUDE.md 갱신).

## 3. 결정과 근거
- API·DB까지 재설계, 공개 사이트 화면은 유지 — 소유자 결정. 26일 남았으므로 마이그레이션은 추가만.
- 알림톡 개인별: 신청 즉시 결제요청 → 입금 확인 즉시 확정 → 사전질문은 max(입금, 행사-28일). 행사-28일=9/26 < 모집 시작 9/28이라 이번엔 사실상 입금 즉시.
- 마이페이지 로그인 없음 — 토큰 링크가 신원. 알림톡이 항상 진입점이라 계정이 필요 없음. 분실 시 전화+생년으로 재발송.
- 알림톡은 카카오 공식 딜러사 필수(카카오 가이드). 딜러사 = API 통로, 대행 아님.
- 사업 값은 현재 웹사이트가 기준.

## 4. 아직 안 된 것 · 알려진 문제
- `~/projects/hangyeol` 폴더 삭제 (이 세션의 cwd라 못 지움). 명령: `rm -rf ~/projects/hangyeol`
- 자동 메모리를 `~/.claude/projects/C--Users-hyund-projects-hangyeol-web/memory/`로 복사해 둠 — 새 세션에서 폴더명이 다르면 옮길 것.
- `git add` 시 `.claude/skills/neon/`, `.claude/skills/neon-postgres/` "could not open directory" 경고 — 깨진 항목이 있는지 확인 후 정리.
- 확인된 라이브 버그 #9(슬롯 이중 확보)는 아직 안 고침.
- KB 템플릿 T1은 "D-28 일괄" 전제 → 개인별로 바뀐 것을 대표(이현우)에게 알려야 문구가 확정됨.

## 5. 돌고 있던 에이전트 · 결과 요약
- 없음. (코드베이스 지도 에이전트는 완료, 결과는 `docs/architecture.md`.)

## 6. 사용자에게 물어봐야 할 것
- 알림톡 딜러사 선택 (#4 조사표 후).
- `/pre`·`/q`·현장 `/r`을 `/me/[token]` 하나로 합칠지 (#7).
- IBK 계좌번호·통신판매업 신고번호 — 대표.

## 7. 다음 세션의 첫 행동 (명령 그대로)
```
cd ~/projects/hangyeol_web
gh issue list --label P0 --milestone "9/28 모집 시작"
```
순서: #2 잔여 체크 → #9 핫픽스(작고 확인된 버그, 브랜치 `동현-9-슬롯이중확보`) → #8 모션그래픽 → 설계 이슈 #6 → #3 → #4 → #7 순으로 문서 작성·승인.
