# 핸드오프 2026-09-05

> 2026-09-04 이후 **코드 변경 없음.** 이 문서는 `2026-09-04.md`의 상태를 이어받아 첫 행동만 갱신한 것이다.
> 상세 근거(#8 결정 4건, riseIn 함정, 게이트 레이어 높이 문제)는 `docs/handoff/2026-09-04.md` §3·§4를 읽는다.

## 1. 지금 하던 이슈
- 진행 중인 구현 없음. 작업 트리 깨끗, `main` = `origin/main`.
- 다음은 **설계 이슈 #6**(`docs/erd.md` + `docs/api.md`). 코드보다 문서 먼저, 승인 후 구현.

## 2. 이번 세션에서 끝난 것
- 없음(코드). 2026-09-02 계획 세션의 마감 절차만 정리했다.
- ⚠️ 실수 1건 바로잡음: 옛 핸드오프 `2026-09-03.md`를 `2026-09-05.md`로 이름만 바꿔 푸시했다가(`875b96f`)
  훅이 낡은 문서를 최신으로 주입하게 된 것을 발견 → 되돌리고 이 문서를 새로 썼다.
  **교훈: 핸드오프를 쓰기 전에 `git log`와 `docs/handoff/`를 먼저 본다. 다른 세션이 이미 진행했을 수 있다.**

## 3. 결정과 근거 (누적, 최신순)
- 2026-09-04 `docs/decisions/001` — 72시간 기준점은 1단계 폼 제출 시각 유지 · T1 문구 「3일」 고정 · 10/15~17 제출자는 소유자가 개별 안내.
- 2026-09-03 #8 — mp4를 `<video>`로 붙이지 않고 움직임만 벡터로 이식(사파리 알파·로고 비율·해상도). 최대 배율 1.06.
- 2026-09-03 계획 — API·DB까지 재설계, 공개 화면 유지 · 알림톡 개인별 · 마이페이지 로그인 없음(토큰) · 딜러사 필수.
  전체 계획 `C:/Users/hyund/.claude/plans/1-generic-patterson.md`.

## 4. 아직 안 된 것 · 알려진 문제 (2026-09-04에서 이월)
- 🔴 `src/components/HomeHero.module.css`의 `riseIn` 3곳이 CSS Module 지역화로 죽어 있음 — 살리면 등장 시각이 바뀌므로 소유자 결정 대기.
- 게이트 레이어가 짧은 가로 화면(740×420)에서 아래로 밀림 — #8 이전부터. 별도 이슈로 만들지 결정 필요.
- `npm run lint` 오류 2건: `.claude/hooks/handoff-inject.js`의 `require()`. 훅 파일을 ESLint 대상에서 빼거나 ESM으로 바꿀 것.
- `.claude/skills/neon`, `neon-postgres` → `.agents/skills/…` 깨진 심볼릭 링크(git 경고). 삭제 대상.
- #2 잔여는 전부 외부 대기: 통신판매업 신고번호(→ `HANGYEOL_BIZ_MAILORDER`), `NEXT_PUBLIC_META_PIXEL_ID`(선택), 프로덕션 Neon `schema_migration` 기록 확인(확인 전 `npm run db:migrate` 금지).
- KB 알림톡 템플릿 T1이 "D-28 일괄" 전제 → 개인별로 바뀐 것을 대표에게 알려야 문구 확정.
- `~/projects/hangyeol`(옛 세션 폴더) 삭제: `rm -rf ~/projects/hangyeol`.

## 5. 돌고 있던 에이전트 · 결과 요약
- 없음.

## 6. 사용자에게 물어봐야 할 것
- `HomeHero.module.css` 죽은 `riseIn` 3곳을 살릴지(건너뛰기 화면 CTA가 4.6초쯤 등장하게 됨), 별도 이슈로 뺄지.
- 게이트 최대 배율 1.06이 영상보다 심심한지.
- 알림톡 딜러사 선택(#4 조사표 후) · `/pre`·`/q`·`/r`을 `/me/[token]`로 합칠지(#7) · IBK 계좌번호·통신판매업 신고번호(대표).

## 7. 다음 세션의 첫 행동 (명령 그대로)
```
cd ~/projects/hangyeol_web
git pull
gh issue view 6
```
순서: #6 → #3 → #4 → #7 (설계 문서, 각각 승인) → 구현 #11 → #12 → #13 → #14 → #15 → #16.
