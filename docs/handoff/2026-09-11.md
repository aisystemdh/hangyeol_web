# 핸드오프 2026-09-11

> 이번 세션은 **코드를 한 줄도 안 고쳤다.** 조사 → 계획 승인 → **정본 문서 6개 갱신**까지다.
> 계획은 `C:\Users\hyund\.claude\plans\merry-bubbling-meerkat.md` 에 있고 **승인됐다.**
> 다음 세션은 그 계획의 **단계 1(알림톡 알리고 전환 + 문구 교체)**부터 구현을 시작한다.

## 1. 지금 하던 이슈

- **정식 이슈 번호 없음.** 열린 이슈는 `#28`(스펙 — 운영 시스템 전면 재설계) 하나뿐이다.
  이번 계획은 그보다 범위가 넓다. **다음 세션 첫 일 중 하나가 이슈 쪼개기다.**
- 브랜치: `main`. 작업 트리에 **문서 변경만** 있다(코드 변경 0).
- 어디까지: 조사와 설계가 끝났고 문서에 박혔다. 구현은 시작 전.

## 2. 이번 세션에서 끝난 것

문서 여섯. 코드 커밋 없음.

| 파일 | 무엇 |
|---|---|
| `docs/decisions/004-aligo-cumulative-mypage-2026-09-11.md` | **신규.** 이번 결정 11건의 정본 |
| `docs/decisions/003-…-2026-09-05.md` | 머리말에 **대체된 네 곳 대조표** 추가 |
| `CONTEXT.md` | 「닉네임」·「번호」 분리 · 「마이페이지」 누적형 · 「알림톡」→「안내 열 가지」 · 새 말 넷 · 쓰지 않는 말 셋 |
| `docs/alimtalk-templates.md` | **전면 재작성.** 문구 전문을 빼고 **지식베이스를 정본으로 가리키는 지도**로 |
| `CLAUDE.md` | 004 추가 · 대행사 · 지식베이스 정본 예외 둘 · `/me`·`/admin` 현행화 · `event.ts` 불일치 셋 경고 |
| `docs/architecture.md` | 004 예고 절 추가(무엇이 없어지고 생기는지) |

🔴 **`docs/alimtalk-templates.md`에서 문구 아홉 개 전문을 지웠다.** 지식베이스 쪽이 정본이 됐고,
두 벌이 되면 반드시 하나가 낡기 때문이다. 2026-09-05 판이 실제로 그렇게 낡아 있었다.

## 3. 결정과 근거

**소유자 지시(이번 세션 발언 기준)**

- **알리고 명세대로 API를 고치고, 문구를 바뀐 것으로 교체하고, 없어진 것은 지운다.**
- **템플릿 등록·심사 요청 기능을 넣는다.** 지금 여섯 개는 **이미 알리고 화면에서 손으로 넣어
  심사가 돌고 있다.** 그래도 앞으로 편하니 만든다. 🔴 **심사중·반려 같은 상태값을 볼 수 있게 한다.**
- **「현장에서 쓸 폼」과 「마음에 든 분 남기기 → 연결 결과」도 만든다.** 폼은 지식베이스에 있다.
- 🔴 **손님이 자기 닉네임을 직접 정하게 하고 DB에 저장한다.**
- **마이페이지를 누적형으로** — 단계가 나아갈수록 이전 것을 포함해 볼 수 있는 것이 많아진다.

**설계 결정과 근거** (전문은 `004`)

- **마이페이지를 별도 배포하지 않는다** — 계좌·문항을 막는 장치가 이 레포에 묶여 있어
  쪼개면 규칙이 두 벌이 된다. 주소가 바뀌면 알림톡 여섯을 다시 심사받는다.
- **닉네임은 번호를 남기고 칸을 하나 더 만든다** — 번호에 명찰 인쇄와 짝 맞추기가 기댄다.
- **대기 명단 안내는 문자지만 자동이다** — 대기자가 신청 직후 깜깜하면 안 된다.
- **기한 임박도 자동이다** — 사람마다 기한이 달라 손으로 챙기면 반드시 빠진다.
- **도달 확인을 만들지 않는다** — 알리고는 웹훅이 없고 확정까지 최대 24시간이다.
- **환불 4단계는 확정으로 보고 진행한다** — 대표 법률 문서와 지식베이스 운영표가 같은 금액이다.

## 4. 아직 안 된 것 · 알려진 문제

### 🔴 지금 코드와 사실이 어긋난 곳

| 무엇 | 코드 | 사실 |
|---|---|---|
| 행사 장소 | `src/lib/event.ts:32` `합정` + `src/app/events/1/page.tsx:28,46` | 서울 강남구 봉은사로 206 3층 (언주역) |
| 환불 규정 | `src/lib/event.ts:83` 3단, 마지막이 「환불이 어렵습니다」 | 4단. 불참해도 3,900원 환불 |
| 알림톡 시간 변수 | `EVENT.time` = `18:00–21:00` | `18:00 (17:30 입장)` |
| 알림톡 문구 | `db/migrations/006_templates.sql` 아홉 개 · 버튼 있음 · 링크만 | 열 개 · **버튼 없음** · 링크 앞 고정값 |
| 대행사 | `src/lib/alimtalk.ts:130-257` 뿌리오 | 알리고 |
| 도달 웹훅 | `src/app/api/alimtalk/result/route.ts` | 알리고는 **밀어 주지 않는다.** 이 라우트는 죽는다 |
| 법률 페이지 | 없음 | `/terms`·`/privacy` 둘 다 있어야 한다 |
| 푸터 사업자 정보 | 없음(`biz.ts`는 마이페이지 두 화면에서만 쓴다) | 전자상거래법 §10① 표시 의무 |

### 🔴 운영자 화면에 없는 것 (신청 정보를 손으로 못 고친다)

`src/app/api/apply/route.ts:174` 주석이 「잘못 적은 신원은 운영자가 고친다(#34)」고 적어 두고
기대는데 **그 기능이 없다.** 없는 것 전부: 신청자 정보 수정 폼 · 취소 되돌리기 ·
기한 연장 · 닉네임 개별 수정 · 리포트 등록 화면(API는 완성돼 있고 부르는 버튼만 없다) ·
운영자가 신청을 만들거나 지우는 길. `/admin` 목록의 일괄선택 체크박스도 아무 데도 안 이어져 있다.

### 지금 없어서 막히는 값

- 알리고 **사용자 아이디**와 **등록 발신번호** — 받은 것 둘뿐이고 이 둘이 없다.
- 🔴 **알리고 잔여 포인트 0** — 충전 전에는 한 통도 안 나간다.
- **승인된 템플릿 코드 여섯 개** — 심사 통과 후.
- `CRON_SECRET`이 `.env.local`에 없다 — 파기 배치와 새로 만들 기한 알림 배치가 이걸로 막힌다.

### 이월

- `src/components/HomeHero.module.css:29,47,65`의 `riseIn` 3곳이 CSS Module 지역화로 죽어 있음.
- 게이트 레이어 짧은 가로 화면(740×420) 밀림.
- `~/projects/hangyeol`(옛 세션 폴더) 삭제 대기.
- `@types/node` 20→24 올린 것 소유자 확인 대기.
- 🔴 **지식베이스 로컬 체크아웃이 `771a213`(2026-08-30)에 멈춰 있다.** 원격 `main`은 그보다 훨씬 앞서 있고
  `8_알림톡` 폴더가 **로컬에 아예 없다.** 다음 세션은 `git pull`을 먼저 하거나 `gh api`로 읽는다.

## 5. 돌고 있던 에이전트 · 결과 요약

이번 세션 서브에이전트 다섯, 전부 완료. 지금 도는 것 없음. 핵심만.

1. **마이페이지 구조** — 주소 하나(`/me/[token]`)에서 `src/lib/me-screen.ts:86-103`이 화면
   여덟 중 **맞는 첫 하나**를 고르고, `:115-128`이 노출을 한 번 더 막는다.
   클라이언트는 `src/components/MeClient.tsx:107-142`에서 `switch` 하나로 그린다.
   🔴 주석에 **「정식등록을 마친 취소자에게서 계좌가 샌 조합이 실제로 있었다」**가 적혀 있다 —
   누적형으로 바꿀 때 이 조합을 반드시 다시 검사한다.
2. **알리고 명세**(공식 문서 원문 실측) — 알림톡과 문자가 **다른 호스트·다른 인증 파라미터
   이름·다른 성공 코드값**을 쓴다. HTTP는 실패해도 항상 200. **웹훅 없음.**
   본문이 템플릿과 **개행까지 같아야** 하고, 다르면 도달 조회에서만 드러난다.
   토큰 발급 절차가 **현재 문서에서 통째로 삭제**돼 있으나 과거 스냅샷에는 필수였다 — 보내는 쪽이 안전.
   템플릿 등록·수정·삭제·**심사 요청**이 전부 API로 되고, 심사 요청은 **API로만** 된다.
3. **지식베이스 알림톡** — 열 개 중 **알림톡 여섯만** 심사에 올린다.
   🔴 **①이 카카오에서 실제로 반려됐다**(사유: 링크가 결제 링크로 보인다).
   고쳐서 재심사 넣은 상태. **열 개 전부 링크 앞에 고정값이 붙었다.**
   앞선 핸드오프의 "10건 중 9건 반려"는 심사 전 자체 검수였다 — **카카오 통지가 아니다.**
4. **대표 법률 문서**(바탕화면 `한결 웹사이트 수정본`, 2026-09-10) — 이용약관 15조와
   개인정보처리방침 12조 **전문**이 와 있어 새로 쓸 글이 없다. 푸터 사업자 정보,
   환불 4단계, 2부 상대 배정 고지, 장소·부가세·「영구」→1년·현금영수증까지 16개 항목.
   ⚠️ 경로가 `C:\Users\hyund\OneDrive\바탕 화면\…`이고 **파일명이 한글 자모 분해 방식**이라
   Read와 `cat`이 전부 실패한다. PowerShell `Get-Content -LiteralPath`로만 읽힌다.
5. **지식베이스 현장 폼** — 폼 10·11·12·13이 「현장에서 쓸 폼」, 14가 「마음에 든 분 남기기」.
   **종이 폼은 하나도 없다**(전부 참가자 본인 폰). 좋아요는 **닉네임 문자열**로 상대를 고르고
   서버가 **반대 성별 열 명만** 내려준다.
   🔴 **닉네임은 지식베이스 세 곳에서 「무작위 배정 · 번호로 성별을 맞힐 수 없어야 한다」**로
   못박혀 있다 — 소유자 지시와 정면으로 부딪친다. `004` §4에 그대로 적었다.
   🔴 **「연결 결과」 화면 사양이 지식베이스 어디에도 없다.**

## 6. 사용자에게 물어봐야 할 것

1. 🔴 **닉네임을 손님이 정하면 성별 은폐가 깨진다.** 지식베이스가 세 곳에서 막고 있는 규칙이다.
   그래도 진행할지, 아니면 번호를 그대로 쓰고 별명만 현장 호칭으로 쓸지.
   그리고 지식베이스 쪽 문서(운영 매뉴얼 §2.4, 사양집 §1)를 누가 고칠지.
2. 🔴 **「연결 결과」 화면에 무엇을 띄울지.** 사양이 없다. 재료는 운영 매뉴얼의 매칭 통보 문안 둘뿐이다.
3. 🔴 **공개 도메인** — `hangyeolofficial.kr`(실제·관공서 제출)인가 `hangyeol.kr`(문서의 이상형)인가.
   **심사를 더 걸기 전에 정해야 한다.**
4. 🔴 **사전 질문 답을 행사 30일 뒤에 지울지.** 새 처리방침은 그렇게 적었고, 003 결정 16은
   선택지 답을 남긴다고 정했다. **처리방침을 올리는 순간 30일이 법적 약속이 된다.**
5. 🟡 **문자 대체발송을 켤지.** 문서는 켜라 하고 지난 기록에는 끈다고 답하신 것으로 남아 있다.
6. 🟡 **좋아요 마감 시각** — 약관은 48시간, 알림톡 문구는 「내일 밤까지」다.
7. 🟡 **「채널 추가」 체크**를 어느 문구에 켤지(지식베이스는 ①과 ⑨만 권한다).
8. 알리고 **아이디·발신번호**와 **포인트 충전**. 그리고 `CRON_SECRET`.
9. 대표가 동현님께 직접 물은 것 둘 — **Vercel 개인정보 연락처가 `privacy@vercel.com`이 맞는지**,
   **첫 화면 3문항 답에 세션 아이디나 접속 IP가 함께 저장되는지**(코드를 보고 답을 만들어 드리기로 했다).

## 7. 다음 세션의 첫 행동 (명령 그대로)

```bash
cd ~/projects/hangyeol_web
git pull
npm test                                             # 19개 통과가 기준선
cd ~/projects/hangyeol-knowledge-base && git pull    # 로컬이 크게 뒤처져 있다
```

그다음:

1. **계획서를 읽는다** — `C:\Users\hyund\.claude\plans\merry-bubbling-meerkat.md` (승인됨).
   정본 결정은 `docs/decisions/004-aligo-cumulative-mypage-2026-09-11.md`.
2. **§6의 1·2·3번을 먼저 묻는다** — 닉네임, 연결 결과 화면, 도메인.
   셋 다 구현 도중에 바뀌면 되돌리기가 비싸다.
3. **GitHub 이슈로 쪼갠다.** 단계 1~7이 그대로 이슈 후보다. `#28`은 이 계획으로 대체되므로 닫는다.
4. **단계 1부터 구현한다** — 브랜치 `동현-<이슈번호>-알리고전환`.
   순서: 발송 통로 교체 → 웹훅 라우트 삭제 → 버튼 걷어내기 → 문구 새 마이그레이션 →
   채널 분기 → 기한 임박 배치 → 테스트 재작성.
   🔴 **키가 없어도 문구 교체와 코드 전환은 지금 할 수 있다.** `ALIMTALK_PROVIDER=fake`로 둔다.

⚠️ **범위가 크다. 마감이 둘이다** — 단계 1·2는 **모집 시작 전**, 단계 3·4는 **행사(10/24) 전**.
단계 1을 끝내고 한 번 끊어 확인받는 편이 안전하다.
