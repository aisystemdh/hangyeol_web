# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# 🔒 대외비 — 절대 커밋 금지 (협업자에게도 노출되면 안 됨)
# 한 번 푸시되면 히스토리에서 지우기 매우 어렵다. 여기 등록된 상태를 유지할 것.
01_brand_philosophy.md

# Claude Code — 개인 설정만 제외. settings.json·skills·hooks는 팀과 공유한다
.claude/settings.local.json

# 에이전트 병렬 작업용 임시 git worktree — 레포 전체 사본이 통째로 들어있다.
# 공유 설정이 아니라 실행 중 산출물이라 팀 공유 대상에서 뺀다(2026-09-06 실측).
.claude/worktrees/
