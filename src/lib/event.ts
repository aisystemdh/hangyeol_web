/**
 * 1차 모임의 확정 사실.
 * 시각·정원·장소의 원본은 옵시디언 볼트 `C:\second_brain\한결\_1차 오프라인 소개팅.md`와
 * 기획완성본의 진행표다. 값을 바꾸기 전에 반드시 원본을 먼저 확인할 것 —
 * 광고와 실제가 다른 것이 이 시장의 대표적 불만 요인이다.
 *
 * 🔴 단, **나이 범위·미혼 조건·환불 3단은 위 볼트에 없다**(2026-08-25 실측 — 그 파일에
 *    `미혼`·`환불`·연령 범위가 한 번도 나오지 않는다). 웹이 먼저 정한 값이며,
 *    정본으로 승격할지는 소유자 결정 사항이다. **볼트를 근거로 인용하지 말 것.**
 */
export const EVENT = {
  /**
   * 회차 번호. 🔴 DB `event` 표의 `id`와 **같은 값이어야 한다** — 신청과 돈 줄이
   * 이 값으로 회차에 붙는다. 2차를 열 때 마이그레이션에 회차 행을 더하고 여기를 올린다.
   * (데이터 모델은 회차를 여럿 담지만 이번에 쓰는 것은 1차뿐이다.)
   */
  id: 1,
  /**
   * 🔴 **기계가 읽는 행사일.** 입금 기한·환불 경계·전체 마감이 전부 여기서 계산된다.
   *    날짜가 바뀌면 이 한 줄만 고친다 — 다른 곳에 절대 날짜를 박지 않는다.
   *    (2026-08-29 소유자 확정. 토요일이다.)
   */
  dateISO: "2026-10-24",
  /** 화면에 보이는 표기. ⚠️ dateISO와 같은 날을 가리켜야 한다.
   *  히어로 칩·하단 고정바처럼 좁은 자리에도 들어가야 하므로 요일은 괄호로 줄여 붙인다. */
  date: "10월 24일 (토)",
  time: "18:00–21:00",
  /** ⚠️ 17:45로 되돌리지 말 것 — 기획완성본은 17:30이다(3시간, 17:30 입장 ~ 21:00).
   *  15분이 아니라 30분인 이유는 그 사이에 20명분 **개인 고유 링크를 문자로 발송**하기
   *  때문이다. 15분으로 공지하면 늦게 오는 사람이 생기고 발송이 밀린다. */
  doorsOpen: "17:30 입장",
  place: "합정",
  placeNote: "신청자에게 개별 안내",
  capacity: 20,
  capacityPerGender: 10,
  priceLabel: "39,000원",
  priceNote: "남녀 동일",
  ageMin: 20,
  ageMax: 32,
  /**
   * 1대1 대화 횟수·시간. 출처: 볼트 `_1차 오프라인_타임테이블.md`
   * (1대1 대화 5회+5회, 1명당 이동 1분 + 대화 9분).
   * ⚠️ 화면 카피에 "로테이션"이라는 단어는 쓰지 않는다 — 노출 금지 내부 용어다.
   */
  rotationPartners: 10,
  rotationMinutes: 9,
} as const;

/**
 * 구조화 데이터(schema.org `Event`)가 읽는 값. `/events/1`의 JSON-LD만 쓴다.
 *
 * 🔴 **날짜는 여기에 다시 적지 않는다** — 위 `dateISO` 하나에서 파생된다.
 * ⚠️ 시각·금액은 위 `time`·`priceLabel`을 사람이 읽는 형태로 적어 둔 것이라
 *    기계용으로 한 번 더 쓴다. **셋을 반드시 함께 고칠 것.** 화면의 줄과 검색 결과에
 *    실리는 값이 어긋나면 표시광고법상 문제가 되는 쪽은 후자다 — 방문자는 검색 카드를
 *    보고 오는데 그 값은 우리가 구글에 직접 신고한 것이기 때문이다.
 */
export const EVENT_SCHEMA = {
  /** `time`의 앞 시각. */
  startISO: `${EVENT.dateISO}T18:00:00+09:00`,
  /** `time`의 뒤 시각. */
  endISO: `${EVENT.dateISO}T21:00:00+09:00`,
  /** `priceLabel`의 금액에서 쉼표와 「원」을 뺀 것. schema.org는 숫자만 받는다. */
  priceKRW: "39000",
} as const;

/**
 * 환불 규정. 카피에 문장을 직접 쓰지 말고 여기서 가져온다.
 * ⚠️ 이 3단은 **행사일** 기준이다. 아래 REFUND_LAW와 반드시 함께 노출한다.
 */
export const REFUND = [
  { when: "7일 전", what: "전액 돌려드립니다." },
  { when: "3일 전", what: "절반을 돌려드립니다." },
  {
    when: "그 이후",
    what: `환불이 어렵습니다. 자리가 ${EVENT.capacity}석뿐이라 한 명이 빠지면 성비가 무너지기 때문입니다.`,
  },
] as const;

/**
 * 법정 청약철회권 — 위 REFUND에 얹히는 예외 두 줄.
 *
 * 🔴 **지우지 말 것.** 위 REFUND는 **행사일**이 기준이고 청약철회권은 **신청·결제일**이
 *    기준이라 축이 다르다. 행사 5일 전에 신청한 사람이 다음 날 취소하면 위 표로는
 *    「절반」 구간이지만 법으로는 전액 철회 대상이다(전자상거래법 제17조 ①).
 *    같은 법 제35조에 따라 **소비자에게 불리한 약정은 그 부분이 무효**이므로,
 *    이 두 줄이 없으면 그 구간에서 위 표가 효력을 잃는다.
 * ⚠️ 문구만 두는 것으로 끝나지 않는다 — 운영 환불 절차에도 같은 조건이 있어야 한다.
 * ⚠️ 변호사 검증 전 초안이다(2026-08-24 법적 점검 문서 §3).
 */
export const REFUND_LAW = [
  "신청하신 날부터 7일 이내이고 행사가 아직 시작되지 않았다면, 위 규정과 관계없이 전액 돌려드립니다. 「전자상거래 등에서의 소비자보호에 관한 법률」 제17조 제1항에 따른 것입니다.",
  "최소 인원이 모이지 않아 저희가 취소하는 경우에도 전액 돌려드립니다.",
] as const;

/**
 * 카피에서 반복해 쓰는 문구. 숫자를 문장에 박아 넣지 않기 위한 헬퍼다.
 * 참가 조건·FAQ·폼 검증 메시지가 전부 이 한 줄을 쓴다 — 나이가 바뀌면 여기만 고친다.
 */
export const AGE_RANGE = `${EVENT.ageMin}살~${EVENT.ageMax}살`;

/**
 * 참가 조건 한 줄 — 정보표에 그대로 쓴다.
 * ⚠️ 「미혼」을 접이식 안쪽에만 두지 말 것. 허위 기재의 책임을 참가자에게 물으려면
 *    조건이 눈에 띄게 고지돼 있어야 한다 — 접어두면 "몰랐다"는 항변이 가능해진다.
 */
export const CONDITION_LABEL = `${AGE_RANGE} · 미혼`;
